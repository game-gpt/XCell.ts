{
    "private": true,
    "scripts": {
        "build": "pnpm -r --filter './compilers/*' run build",
        "build:runtimes": "pnpm -r --filter './runtimes/*' run build",
        "test": "pnpm -r run test",
        "typecheck": "pnpm -r run typecheck",
        "fmt": "biome format --write .",
        "lint": "biome lint .",
        "check": "biome check ."
    },
    "devDependencies": {
        "@biomejs/biome": "^1.9.4",
        "@types/node": "^22.0.0",
        "typescript": "^5.7.0"
    }
}
